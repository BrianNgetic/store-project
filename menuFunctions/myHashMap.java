package menuFunctions;

import java.util.*;
import products.Electronics;




public class myHashMap{

    int id = 1456;

    public  myHashMap() {
        //initialize a hashmap

        HashMap<String, Integer> ProdIds = new HashMap<>();
        //to work on in a bit

        Electronics e = new Electronics(); //instantiate a non static class
        e.ReadfromElectronicFile(); 

        for(int i  = id; (line  = ReadfromElectronicsFile.readLine()) != null);i+){

        // }
    }
}

